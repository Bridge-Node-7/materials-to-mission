# Versioning

This repository follows Semantic Versioning.

- Patch releases correct defects without intentionally changing the public data contract.
- Minor releases add backward-compatible schemas, commands, examples, or documentation.
- Major releases may change required fields, semantic rules, or compatibility guarantees.

The initial `0.x` series is experimental. Public schemas remain versioned and
changes are documented in `CHANGELOG.md`.
